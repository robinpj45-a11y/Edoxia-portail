import { initializeApp } from "firebase/app";
import { getFirestore, collection, getDocs, setDoc, doc } from "firebase/firestore";

// Copiez votre config firebase ici si nécessaire ou on peut utiliser un fichier local si on l'importe.
// Mais pour être sûr on peut le faire directement depuis le composant React ou un composant de montage ponctuel.
